# Extra dependencies for the visual theme designer (theme-designer.py) ONLY.
#
# Deliberately NOT in requirements.txt: the system monitor has to stay light
# enough to run alongside a game, and there is no reason to push ~100 MB of Qt
# onto someone who only wants their panel driven.
#
#   pip install -r requirements.txt -r requirements-editor.txt
#
# PySide6 is LGPLv3, which is compatible with this project's GPL-3.0-or-later.
# 6.11+ ships cp310-abi3 wheels, so it works on Python 3.14.
PySide6~=6.11.2
